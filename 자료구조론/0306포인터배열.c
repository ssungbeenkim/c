#include <stdio.h>

/* 
& : 주소 연산자로서 변수의 주소를 알아내는 연산자
* : 참조 연산자로서 포인터 저장된 주소에 있는 값을 엑세스하는 연산자 



 */



void main() {
	char* ptrArray[2]; // 포인터 배열
	char** ptrptr; // 이중 포인터 
	int i;

	printf()

	ptrArray[0] = "Korea"; 
	ptrArray[1] = "Seoul"; 

	ptrptr = ptrArray;


return 0;
}